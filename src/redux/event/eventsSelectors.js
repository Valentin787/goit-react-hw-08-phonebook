export const eventsState = (state) => state.events.data.item;
export const loadingSelector = (state) => state.events.data.loading;
export const errorSelector = (state) => state.events.data.error;
// export const filterPhoneBook = (state) => state.contacts.filter;
